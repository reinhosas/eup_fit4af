<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<p>
  <strong>Start date:</strong>
  <%= @project.start_date %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
<%= link_to "employees", project_employees_path(@project.id), class: "button" %>

<!-- 
<tbody>
    <% @employeesshow.each do |var| %>
      <tr>
        <td><%= var.vorname %></td>
        <td><%= var.nachname %></td>
        
    <% end %>
  </tbody>
</table>
 -->

