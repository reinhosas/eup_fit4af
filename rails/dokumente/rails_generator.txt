Anlegen eines Controllers:
	rails g controller events

Anlegen eines Models:
	rails g model event name:string location:string price:decimal
		#Das Model Event(Tabelle: events) wird mit den Spalten:
		name, location, price angelegt
		